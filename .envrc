# Load variables from .env into your shell when you enter this directory (requires direnv: https://direnv.net/).
# Uses direnv’s built-in dotenv support — no npm `dotenv` package.
dotenv_if_exists .env
